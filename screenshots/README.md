# Screenshots

Place exported dashboard / pipeline screenshots here and reference them from the
root `README.md`. Suggested set:

| File | What it shows |
|------|---------------|
| `architecture.png` | The end-to-end architecture diagram |
| `overview.png` | Power BI — Real-Time Overview page (KPI cards, line/bar/pie) |
| `data_quality.png` | Power BI — Data Quality page (DLQ rate, error reasons) |
| `spark_ui.png` | Spark Structured Streaming UI (running queries) |
| `kafka_ui.png` | Kafka UI — topics and throughput |
| `bigquery.png` | BigQuery — partitioned/clustered tables with data |

> These are placeholders; capture them after running the pipeline end-to-end.
